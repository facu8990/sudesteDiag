name: Create Archive
on:
  push:
    branches:
    - master
jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: Klemensas/action-autotag@v2
      with:
        GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
